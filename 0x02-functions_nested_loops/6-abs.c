#include "main.h"

/**
 * _abs - compute absloute value of integer
 * @i: integer value
 * Return: absolute value
 */

int _abs(int i)
{
	if (i > 0)
		return (n);
	else if (n < 0)
		return (-n);

	return (0);
}
