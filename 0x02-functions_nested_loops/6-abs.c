#include "main.h"

/**
 * _abs - compute absloute value of integer
 * @i: integer value
 * Return: absolute value
 */

int _abs(int i)
{
	if (i > 0)
	{
	return (i);
	}
	else if (i < 0)
	{
	return (-i);
	}

	return (0);
}
